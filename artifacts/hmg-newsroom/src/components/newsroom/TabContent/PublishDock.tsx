/**
 * PublishDock — thin organizational re-export of the PublishPanel embedded
 * inside Quick + Pack output. Keeps the TabContent folder structure self-
 * describing without duplicating the publish logic.
 */
export { PublishPanel as PublishDock } from "../PublishPanel";
